<%= form_with(model: colour) do |form| %>
  <% if colour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colour.errors.count, "error") %> prohibited this colour from being saved:</h2>

      <ul>
        <% colour.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
